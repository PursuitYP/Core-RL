# 当前结果索引（中文）

本文件是 `final/indexes/results.md` 的中文审阅版。最终引用仍以英文 result index 和各 report 为准；这里帮助快速理解每个结果到底说明了什么。运行中或待完成的 CPU job 不是 evidence；只有包含 `metrics.csv`、`summary.json`、`condition_summary.json`、`config_used.json` 和 `manifest.json` 的结果目录才能进入正式证据口径。

截至 2026-07-09 最新 artifact audit，scale fixed-condition extended job、output-fairness audit、reward-centered sensitivity sweep 和 Dyna drift sweep 都已经写出标准 artifacts，均可作为当前 evidence。旧失败 job IDs `21581151`、`20998802`、`18940577` 在 rjob 容器内报 `/data/yupeng/conda_envs/core-rl/bin/python: No such file or directory`，只作为 audit，不能混入当前证据。

## 主线和综合候选

| Study | 当前结果路径 | 关键结果 | 限制和下一步 |
|---|---|---|---|
| Reward-Centered Sarsa | 主证据：`experiments/alberta_core_rl/results/reward_centered_sarsa/20260709T024517Z_extended`；sensitivity evidence：`experiments/alberta_core_rl/results/reward_centered_sarsa_sensitivity/20260709T085747Z_extended`；recovery analysis：`final/reports/proposals/reward_centered_sarsa/sensitivity_figures/reward_recovery_compact_best_by_family.csv` | 主 20-seed shift/alpha run 显示 reward-centered Sarsa 的 unshifted reward 基本保持 `2.55-2.60`，Q norm 约 `22-35`；discounted Sarsa 在 shift `8`、alpha `0.1` 时 Q norm 约 `1876`，unshifted reward 约 `1.70`。Sensitivity run 包含 10 seeds、`8,112,150` 行 metrics、`1,575` 个 condition groups，所有 conditions 都无 divergence。no-reset switch 下，reward-centered Sarsa 的 post-late 最佳结果为 down-shift `2.6364 +/- 0.0280`、sign-shift `2.6089 +/- 0.0274`、up-shift `2.6057 +/- 0.0273`；differential Sarsa 很接近，而 discounted Sarsa 在 down-shift 较差 condition 中只有 `1.5814 +/- 0.2210`，且 Q norm 可达约 `1350`。新增 early recovery score 分析显示，switch 后前 `1000` 个 logged steps 中 reward-centered best scores 为 down/sign/up `2.549/2.583/2.580`，discounted best 为 `2.025/2.535/2.378`，且 discounted Q norm 仍明显更大。 | sensitivity evidence 对 reward-origin switch 很强，但仍只覆盖 access-control family；recovery score 还不是 matched-behavior paired causal comparison。下一步是 full policy-distance probes、matched-seed recovery analysis 和 natural reward drift。 |
| Output-Controlled TD | Primary evidence：`experiments/alberta_core_rl/results/output_controlled_td/20260709T051934Z_extended`；fairness-audit evidence：`experiments/alberta_core_rl/results/output_controlled_td_fairness_audit/20260709T085746Z_extended`；stability frontier：`final/reports/proposals/output_controlled_td/stability_frontier/` | Primary 20-seed result：normalized TD 和 trace-normalized TD 都是 `0/400` seed-conditions 发散；fixed TD 和 raw-alpha true-online TD(lambda) 都是 `141/400`。Primary max-stable-alpha frontier 显示 normalized 和 trace-normalized TD 在所有测试 scale 上都稳定到 alpha `0.3`，而 fixed 与 raw true-online 没有稳定的 `hundred` cell。Fairness audit：normalized TD 和 trace-normalized TD(lambda) 都是 `0/300` divergent seed-conditions；fixed TD 和 raw-alpha true-online TD(lambda) 都是 `81/300`；naive normalized true-online TD(lambda) 改善到 `34/300`，失败集中在 lognormal feature scaling，并且没有 fully stable 的 lognormal alpha。 | 证据仍是 prediction-side，不是 control-agent result。normalized true-online audit 有信息量，但还不是 principled final true-online output-control derivation；下一步是 no-reset feature-scale switch 和更严谨的 true-online normalization audit。 |
| Scale-Invariant Continuing Control | Fixed-condition evidence：`experiments/alberta_core_rl/results/scale_invariant_continuing_control/20260709T063128Z_extended`；unit-switch evidence：`experiments/alberta_core_rl/results/unit_switching_continuing_control/20260709T024834Z_extended` | Fixed-condition 20-seed grid：normalized Sarsa、normalized reward-centered Sarsa 和 normalized differential Sarsa 都是 `0/1500` divergent seed-conditions；discounted Sarsa 和 reward-centered Sarsa 都是 `500/1500`。Mean tail unshifted reward：normalized reward-centered Sarsa `2.556`，normalized differential Sarsa `2.554`，discounted Sarsa `1.501`，reward-centered Sarsa `1.759`。unit-switch extension 中组合方法避免 catastrophic Q-norm growth，但 harsh abrupt feature-scale switch 仍会让 late reward 降到约 `1.9-2.0`。 | fixed grid 已完成，但 abrupt switch 比 gradual drift 更严苛，稳定性也不等于完全恢复；下一步是 gradual feature-unit drift、recovery AUC 和 policy-distance probes。 |
| Continual Dyna Model Aging | fixed-change evidence：`experiments/alberta_core_rl/results/continual_dyna_model_aging/20260709T024602Z_extended`；drift evidence：`experiments/alberta_core_rl/results/continual_dyna_model_aging_drift/20260709T085809Z_extended`；frontier analysis：`final/reports/integrated/continual_dyna_model_aging/drift_figures/dyna_drift_reward_stale_frontier.csv` | fixed-change 20-seed sweep 显示 budget `20` 下 keep-model late stale-backup rate 为 `0.213 +/- 0.065`；recency aging 在 half-life `250` 时几乎为 `0`，half-life `4000` 时约 `0.020 +/- 0.004`；late reward 最好约 `0.0925`，oracle flush 约 `0.0906`，keep-model 约 `0.0828`。Drift sweep 写出 `1,800,900` 行 metrics 和 `690` 个 condition groups。Abrupt drift 中最高 realistic reward 是 `dyna_5_keep_model`，`0.09598 +/- 0.00112`，但 `dyna_20_recency_error_gate` half-life `100` 是低 staleness frontier point，reward `0.09118 +/- 0.00551`，stale rate `0.00000`。Gradual drift 中最好是 `dyna_20_recency_error_gate` half-life `1500`，`0.09536 +/- 0.00141`，stale rate `0.02092`。Stochastic drift 不支持 aging 普遍优势，最好是 `dyna_5_keep_model`，`0.03370 +/- 0.01237`。 | 结论必须条件化：freshness-aware search control 对 abrupt/gradual drift 有价值，但 reward 排名依赖 planning budget、half-life 和 drift structure。frontier analysis 澄清了 tradeoff，但还不是真正 per-backup causal utility；下一步是 repeated changes、stochastic transition family 和 true planning-utility logging。 |
| Dyna Planning Budget | `experiments/alberta_core_rl/results/dyna_planning_budget/20260709T024602Z_extended` | 20 seeds larger-grid extended run 中，planning budget `20` 的 late reward 对 keep-model 和 flush 都约 `0.072`；但 keep-model late stale-backup rate 仍约 `0.294`，flush 为 `0`。 | 这是独立诊断；不能单独夸大为 reward superiority。 |
| Predictive State Plasticity | `experiments/alberta_core_rl/results/predictive_state_plasticity/20260709T024517Z_extended` | 20 seeds extended first-gate 仍是负向结果。新增 cue-decodability analysis 显示，maze length `8/12/20/30` 下 cue-GVF decoding 约 `0.589/0.589/0.609/0.622`，trace memory 和 oracle 都是 `1.0`，raw observation 是 `0.5`，recurrent GVF 接近 chance。 | 这是独立诊断/负结果：learned cue-GVF 中有一部分 cue 信息可解码，但当前 recurrent GVF state 还不能解决 control-relevant memory。 |
| Useful Predictive Knowledge | Gate-1/Gate-2 evidence：`experiments/alberta_core_rl/results/useful_predictive_knowledge/20260709T102402Z_main`；Gate-3 feature-budget evidence：`experiments/alberta_core_rl/results/useful_predictive_knowledge_budget/20260709T132025Z_main` | Gate-1/Gate-2：raw observation 接近 chance（`0.506/0.511/0.486`），trace 和 oracle 能解决任务，best learned cue decoder 高于 chance（`0.626/0.611/0.638`），但 control accuracy 仍接近 chance（`0.508/0.508/0.495`）。Gate-3：two-feature budget 下 trace memory 仍很强（`0.947/0.927/0.906`），oracle 仍很强（`0.954/0.952/0.924`）；low-TD 和 cue-decoding selector 通常能保留 cue-GVF pair，但 accuracy 仍接近 chance（`0.504/0.501/0.514` 和 `0.519/0.521/0.508`）。 | 这是更强的 principled negative gate：learned predictions 可以包含并被选择为 cue information，但在当前设计中仍不是 Sarsa control 可用的 agent state。下一步应做 oracle-prediction scaling、GVF-output normalization、prediction-to-policy coupling，然后再进入 plasticity/phase-switch stage。 |

## 支持性诊断和负结果

GVF Predictive State 证明当前 recurrent GVF 不能替代 trace/oracle memory；Generate-and-Test 当前 utility 不优于 random replacement；Options transfer claim 被 quarantine；TIDBD-lite 机制可见但不优于 normalized TD；Baird-style 结果作为 off-policy stability warning；Centered TD Diagnostics 是 reward-centering 的独立机制诊断；On-policy Stability Atlas 是 feature-scale sensitivity 的独立诊断；Nonstationary Bandit 与 Streaming Representation 只保留为 sanity/negative diagnostics。

正式引用请使用 `condition_summary.json` 中的 `*_seed_tail` 统计；如果指标是 divergence 这种 binary event，应优先使用从 `metrics.csv` 重新汇总的 seed-level event rate。旧 main pilots 多数为 seeds `0-4`、5000 online steps；带 `_extended` 的结果通常为 seeds `0-19`、20000 online steps。旧失败 rjob 只作为 audit，不能混入当前证据。
