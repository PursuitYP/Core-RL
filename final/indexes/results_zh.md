# 当前结果索引（中文）

本文件是 `final/indexes/results.md` 的中文审阅版。最终引用仍以英文 result index 和各 report 为准；这里帮助快速理解每个结果到底说明了什么。运行中或待完成的 CPU job 不是 evidence；只有包含 `metrics.csv`、`summary.json`、`condition_summary.json`、`config_used.json` 和 `manifest.json` 的结果目录才能进入正式证据口径。

截至 2026-07-09 17:19 HKT，scale、reward-sensitivity、output-fairness 和 Dyna-drift 四个 CPU jobs 都仍为 Running。Output-fairness 日志在 17:24 HKT 再次检查时已经推进到 condition `938/1500`，因此是在正常推进而不是卡住。三个新建 rerun extended 目录 `experiments/alberta_core_rl/results/reward_centered_sarsa_sensitivity/20260709T085747Z_extended`、`experiments/alberta_core_rl/results/output_controlled_td_fairness_audit/20260709T085746Z_extended` 和 `experiments/alberta_core_rl/results/continual_dyna_model_aging_drift/20260709T085809Z_extended` 目前没有标准 artifacts，只能作为状态/审计记录，不能作为 evidence。旧失败 job IDs `21581151`、`20998802`、`18940577` 在 rjob 容器内报 `/data/yupeng/conda_envs/core-rl/bin/python: No such file or directory`，只作为 audit，不能混入当前证据。

## 主线和综合候选

| Study | 当前结果路径 | 关键结果 | 限制和下一步 |
|---|---|---|---|
| Reward-Centered Sarsa | 当前 evidence：`experiments/alberta_core_rl/results/reward_centered_sarsa/20260709T024517Z_extended`；second-round smoke：`experiments/alberta_core_rl/results/reward_centered_sarsa_sensitivity/20260709T084151Z_smoke`；pending rerun 目录：`experiments/alberta_core_rl/results/reward_centered_sarsa_sensitivity/20260709T085747Z_extended` | 当前可引用证据仍是 20 seeds、20000 steps、5 个 reward shifts、3 个 alpha 的 extended run：reward-centered Sarsa 的 unshifted reward 基本保持 `2.55-2.60`，Q norm 约 `22-35`；discounted Sarsa 在 shift `8`、alpha `0.1` 时 Q norm 约 `1876`，unshifted reward 约 `1.70`。second-round beta/gamma/no-reset reward-origin sensitivity smoke 已存在，CPU rerun 正在运行/待完成。 | pending extended 目录还没有标准 artifacts，不能引用为 evidence；等待 rerun 完成后再更新报告和图表。 |
| Output-Controlled TD | 当前 evidence：`experiments/alberta_core_rl/results/output_controlled_td/20260709T051934Z_extended`；fairness audit smoke：`experiments/alberta_core_rl/results/output_controlled_td_fairness_audit/20260709T084151Z_smoke`；pending rerun 目录：`experiments/alberta_core_rl/results/output_controlled_td_fairness_audit/20260709T085746Z_extended` | 当前可引用证据是 20 seeds、20000 steps extended sweep，覆盖 5 个 feature scales 和 4 个 alpha。normalized TD 和 trace-normalized TD 都是 `0/400` seed-conditions 发散；fixed TD 和当前 raw-alpha true-online baseline 都有 `35.2%` seed-conditions 发散。在 scale `hundred` 下，normalized TD final RMSE 对 alpha `0.01/0.03/0.1/0.3` 约为 `0.56/0.53/0.44/0.28`，trace-normalized TD 约为 `0.56/0.54/0.49/0.40`。fairness audit smoke 已存在，CPU rerun 正在运行/待完成。 | 当前 true-online TD(lambda) baseline 不能作为对 true-online TD 的最终判断；fairness audit rerun 没有标准 artifacts 前不能作为 evidence。 |
| Scale-Invariant Continuing Control | 固定条件 pilot evidence：`experiments/alberta_core_rl/results/scale_invariant_continuing_control/20260708T172151Z_main`；unit-switch evidence：`experiments/alberta_core_rl/results/unit_switching_continuing_control/20260709T024834Z_extended`；running 目录：`experiments/alberta_core_rl/results/scale_invariant_continuing_control/20260709T063128Z_extended` | 固定条件下 normalized-centered/differential variants 最稳；no-reset unit-switch 中 fixed discounted Sarsa 在 feature/joint scale switch 后会出现巨大 Q norm 和非零 divergence，组合方法不发散，但 harsh `hundred` scale switch 后 late reward 仍可能降到约 `1.9-2.0`。 | 完整 fixed-condition 20-seed scale grid 正在运行，但 running 目录没有标准 artifacts 前不能作为 evidence。 |
| Continual Dyna Model Aging | 当前 evidence：`experiments/alberta_core_rl/results/continual_dyna_model_aging/20260709T024602Z_extended`；drift smoke：`experiments/alberta_core_rl/results/continual_dyna_model_aging_drift/20260709T084208Z_smoke`；pending rerun 目录：`experiments/alberta_core_rl/results/continual_dyna_model_aging_drift/20260709T085809Z_extended` | 当前可引用证据是 20 seeds extended half-life/budget sweep：budget `20` 下 keep-model late stale-backup rate 为 `0.213 +/- 0.065`；recency aging 在 half-life `250` 时几乎为 `0`，half-life `4000` 时约 `0.020 +/- 0.004`。late reward 最好约 `0.0925`，出现在 recency/error gate half-life `250` 与 recency aging half-life `4000` 附近；oracle flush 约 `0.0906`，keep-model 约 `0.0828`。drift smoke 已存在，CPU rerun 正在运行/待完成。 | 更保守的结论是 freshness-aware search control 稳定减少 stale backups，reward 排名依赖 budget 和 half-life；drift rerun 没有标准 artifacts 前不能作为 evidence。 |
| Dyna Planning Budget | `experiments/alberta_core_rl/results/dyna_planning_budget/20260709T024602Z_extended` | 20 seeds larger-grid extended run 中，planning budget `20` 的 late reward 对 keep-model 和 flush 都约 `0.072`；但 keep-model late stale-backup rate 仍约 `0.294`，flush 为 `0`。 | 这是独立诊断；不能单独夸大为 reward superiority。 |
| Predictive State Plasticity | `experiments/alberta_core_rl/results/predictive_state_plasticity/20260709T024517Z_extended` | 20 seeds extended first-gate 仍是负向结果。新增 cue-decodability analysis 显示，maze length `8/12/20/30` 下 cue-GVF decoding 约 `0.589/0.589/0.609/0.622`，trace memory 和 oracle 都是 `1.0`，raw observation 是 `0.5`，recurrent GVF 接近 chance。 | 这是独立诊断/负结果：learned cue-GVF 中有一部分 cue 信息可解码，但当前 recurrent GVF state 还不能解决 control-relevant memory。 |

## 支持性诊断和负结果

GVF Predictive State 证明当前 recurrent GVF 不能替代 trace/oracle memory；Generate-and-Test 当前 utility 不优于 random replacement；Options transfer claim 被 quarantine；TIDBD-lite 机制可见但不优于 normalized TD；Baird-style 结果作为 off-policy stability warning；Centered TD Diagnostics 是 reward-centering 的独立机制诊断；On-policy Stability Atlas 是 feature-scale sensitivity 的独立诊断；Nonstationary Bandit 与 Streaming Representation 只保留为 sanity/negative diagnostics。

正式引用请使用 `condition_summary.json` 中的 `*_seed_tail` 统计；如果指标是 divergence 这种 binary event，应优先使用从 `metrics.csv` 重新汇总的 seed-level event rate。旧 main pilots 多数为 seeds `0-4`、5000 online steps；带 `_extended` 的结果通常为 seeds `0-19`、20000 online steps。旧失败 rjob 只作为 audit，不能混入当前证据。
